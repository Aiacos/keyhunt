---
phase: 04-monolith-decomposition
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/modes/modes.h
  - src/modes/mode_address.cpp
  - src/modes/mode_xpoint.cpp
  - src/modes/mode_rmd160.cpp
  - src/keyhunt.cpp
  - Makefile
autonomous: true
requirements: [STR-01, STR-03]

must_haves:
  truths:
    - "ADDRESS mode finds expected keys when dispatched through mode_address.cpp"
    - "XPOINT mode finds expected keys when dispatched through mode_xpoint.cpp"
    - "RMD160 mode finds expected keys when dispatched through mode_rmd160.cpp"
    - "Mode files compile without including keyhunt.cpp or referencing symbols defined only in it"
  artifacts:
    - path: "src/modes/modes.h"
      provides: "Dispatch table interface (mode_ops_t, mode_dispatch)"
      exports: ["mode_ops_t", "mode_get_ops", "mode_dispatch"]
    - path: "src/modes/mode_address.cpp"
      provides: "ADDRESS mode: file reading, bloom setup, GPU init, thread dispatch"
      min_lines: 150
    - path: "src/modes/mode_xpoint.cpp"
      provides: "XPOINT mode: file reading, thread dispatch"
      min_lines: 80
    - path: "src/modes/mode_rmd160.cpp"
      provides: "RMD160 mode: file reading, thread dispatch"
      min_lines: 80
  key_links:
    - from: "src/modes/mode_address.cpp"
      to: "src/search/search_address.cpp"
      via: "platform_thread_create(&tid, thread_process, args)"
      pattern: "thread_process"
    - from: "src/modes/modes.h"
      to: "src/keyhunt.cpp"
      via: "mode_dispatch() called from main()"
      pattern: "mode_dispatch"
---

<objective>
Create the modes.h dispatch interface and extract ADDRESS, XPOINT, and RMD160 mode dispatchers from keyhunt.cpp into src/modes/.

Purpose: These three modes share similar code paths (file reading, bloom setup, thread_process dispatch). Extracting them together establishes the mode extraction pattern for subsequent plans.
Output: src/modes/ with modes.h, mode_address.cpp, mode_xpoint.cpp, mode_rmd160.cpp
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
@/home/aiacos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monolith-decomposition/04-CONTEXT.md
@.planning/phases/04-monolith-decomposition/04-RESEARCH.md
@.planning/phases/04-monolith-decomposition/04-01-SUMMARY.md

<interfaces>
<!-- Research-provided dispatch table pattern -->
From 04-RESEARCH.md Pattern 1:
```cpp
// src/modes/modes.h
typedef struct {
    int (*init)(keyhunt_config_t *config);
    int (*run)(keyhunt_config_t *config,
               platform_thread_t *tids, int *thread_count);
    void (*cleanup)(keyhunt_config_t *config);
} mode_ops_t;

const mode_ops_t* mode_get_ops(search_mode_t mode);
int mode_dispatch(keyhunt_config_t *config);
```

From src/search/search_common.h:
```cpp
struct thread_args { keyhunt_config_t *config; int thread_id; };
// Thread entry points:
platform_thread_return_t PLATFORM_THREAD_CALL thread_process(void *vargp);
```

Key keyhunt.cpp sections to extract:
- Lines 2360-2550: Target file reading (readFileAddress, forceReadFileAddress, bloom setup)
- Lines 2550-2660: Generator point initialization (init_generator, Gn, _2Gn)
- Lines 4525-4600: CPU thread creation switch (case MODE_ADDRESS/XPOINT/RMD160)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create modes.h dispatch interface and mode_address.cpp</name>
  <files>src/modes/modes.h, src/modes/mode_address.cpp, src/keyhunt.cpp, Makefile</files>
  <action>
1. Create src/modes/ directory.

2. Create src/modes/modes.h with the dispatch table interface:
   - `mode_ops_t` struct with init/run/cleanup function pointers
   - `mode_get_ops(search_mode_t mode)` returns ops for a mode
   - `mode_dispatch(keyhunt_config_t *config)` convenience function that calls init+run
   - Include config.h and platform.h
   - run() receives `platform_thread_t **tids_out, int *thread_count_out` so orchestrator can join threads

3. Create src/modes/mode_address.cpp:
   - Move ADDRESS mode initialization from keyhunt.cpp: target file reading (readFileAddress/forceReadFileAddress/forceReadFileAddressEth), bloom filter setup (bloom_check_allocate), endomorphism generator point init, GPU initialization for address mode.
   - `mode_address_init(keyhunt_config_t *config)`: Read target file, set up bloom filter, init generator points, store in config->runtime fields (address_table, address_count, bloom_filter, generator_points, etc.).
   - `mode_address_run(keyhunt_config_t *config, platform_thread_t **tids, int *thread_count)`: Allocate thread handles, create thread_args for each thread, call platform_thread_create with thread_process.
   - `mode_address_cleanup(keyhunt_config_t *config)`: Free allocated thread resources.
   - Absorb ADDRESS-relevant globals into config reads: use `config->search.mode`, `config->runtime.num_threads`, etc.
   - Register mode_address_ops in modes.h dispatch.

4. Update keyhunt.cpp: Remove the ADDRESS mode initialization code that was moved. In main(), after config is built, call `mode_dispatch(&config)` for ADDRESS mode instead of inline code. Keep the monitoring loop in keyhunt.cpp.

5. Update Makefile: Add src/modes/mode_address.o to OBJS.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/keyhunt && make clean all 2>&1 | tail -5 && ./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF 2>&1 | grep -c "KEY FOUND" && echo "ADDRESS mode OK"</automated>
  </verify>
  <done>mode_address.cpp compiles independently. ADDRESS mode finds expected keys via mode dispatch. modes.h dispatch table is functional.</done>
</task>

<task type="auto">
  <name>Task 2: Extract mode_xpoint.cpp and mode_rmd160.cpp</name>
  <files>src/modes/mode_xpoint.cpp, src/modes/mode_rmd160.cpp, src/modes/modes.h, src/keyhunt.cpp, Makefile</files>
  <action>
1. Create src/modes/mode_xpoint.cpp:
   - Move XPOINT mode initialization from keyhunt.cpp: target file reading (forceReadFileXPoint), generator point init.
   - `mode_xpoint_init()`: Read xpoint file, store targets in config->runtime.
   - `mode_xpoint_run()`: Create threads with thread_process (same entry point as ADDRESS, the thread function handles mode internally).
   - Register in modes.h dispatch.
   - XPOINT and ADDRESS share thread_process entry point but differ in file reading and target format.

2. Create src/modes/mode_rmd160.cpp:
   - Move RMD160 mode initialization from keyhunt.cpp: target file reading (readFileAddress for rmd160 binary format).
   - `mode_rmd160_init()`: Read rmd160 file, set up targets.
   - `mode_rmd160_run()`: Create threads with thread_process.
   - Register in modes.h dispatch.

3. Update src/modes/modes.h: Add mode_xpoint_ops and mode_rmd160_ops to the dispatch table. Update mode_get_ops() to return correct ops for MODE_XPOINT and MODE_RMD160.

4. Update keyhunt.cpp: Remove XPOINT and RMD160 initialization code. Route MODE_XPOINT and MODE_RMD160 through mode_dispatch().

5. Update Makefile: Add src/modes/mode_xpoint.o and src/modes/mode_rmd160.o to OBJS.

6. Verify all three modes work:
   - ADDRESS: `./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF`
   - XPOINT: `./keyhunt -m xpoint -f tests/120.txt -t 4 -b 125 -R -q`
   - RMD160: `./keyhunt -m rmd160 -f tests/66.rmd -b 66 -l compress -R -q`
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/keyhunt && make clean all 2>&1 | tail -5 && ./keyhunt -m xpoint -f tests/120.txt -t 2 -b 125 -R -q -s 10 2>&1 | tail -3 && echo "XPOINT OK"</automated>
  </verify>
  <done>mode_xpoint.cpp and mode_rmd160.cpp compile independently. All three modes (ADDRESS, XPOINT, RMD160) find expected keys via mode dispatch. keyhunt.cpp no longer contains their initialization code.</done>
</task>

</tasks>

<verification>
- `make clean all` succeeds
- ADDRESS mode: `./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF` finds keys
- XPOINT mode: `./keyhunt -m xpoint -f tests/120.txt -t 4 -b 125 -R -q -s 10` runs correctly
- RMD160 mode: `./keyhunt -m rmd160 -f tests/66.rmd -b 66 -l compress -R -q -s 10` runs correctly
- `make test` unit tests pass
</verification>

<success_criteria>
- src/modes/ contains modes.h, mode_address.cpp, mode_xpoint.cpp, mode_rmd160.cpp
- All three modes dispatch through modes.h interface
- Mode files do not include keyhunt.cpp or use symbols defined only in it
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-monolith-decomposition/04-02-SUMMARY.md`
</output>
