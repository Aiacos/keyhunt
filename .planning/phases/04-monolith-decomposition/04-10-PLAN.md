---
phase: 04-monolith-decomposition
plan: 10
type: execute
wave: 2
depends_on: ["04-09"]
files_modified:
  - src/keyhunt.cpp
  - src/cli.h
  - src/cli.cpp
  - src/gpu/gpu_dispatch.h
  - src/gpu/gpu_dispatch.cpp
  - src/globals.h
autonomous: true
gap_closure: true
requirements: [STR-01, STR-03, STR-04, STR-05, STR-06, STR-07, STR-08]

must_haves:
  truths:
    - "keyhunt.cpp is 650 lines or fewer (wc -l src/keyhunt.cpp <= 650)"
    - "CLI getopt parsing is in a dedicated function, not inline in main()"
    - "GPU mode resolution logic is in gpu_dispatch module, not inline in main()"
    - "Build passes: make clean && make"
    - "All mode smoke tests pass"
  artifacts:
    - path: "src/keyhunt.cpp"
      provides: "Thin orchestrator: main() calls parse_cli_args(), resolve_gpu_mode(), mode_dispatch(), run_monitoring_loop()"
      min_lines: 300
    - path: "src/cli.cpp"
      provides: "parse_cli_args() function with CLI getopt loop extracted from main()"
      contains: "parse_cli_args"
    - path: "src/gpu/gpu_dispatch.cpp"
      provides: "resolve_gpu_mode() + GPU full search loop + GPU hybrid setup"
      contains: "resolve_gpu_mode"
  key_links:
    - from: "src/keyhunt.cpp"
      to: "src/cli.cpp"
      via: "parse_cli_args() call"
      pattern: "parse_cli_args"
    - from: "src/keyhunt.cpp"
      to: "src/gpu/gpu_dispatch.cpp"
      via: "resolve_gpu_mode() call"
      pattern: "resolve_gpu_mode"
---

<objective>
Extract CLI parsing, GPU mode resolution, GPU full/hybrid search orchestration, and file loading orchestration from main() into dedicated functions, reducing keyhunt.cpp from ~1260 lines (post-plan-09) to ~600 lines or fewer.

Purpose: Close Gap 1 (keyhunt.cpp too large). After plan 04-09 removes ~240 lines of globals, main() is still ~1000 lines. This plan extracts the largest extractable blocks: CLI getopt (~168 lines), GPU mode resolution (~99 lines), GPU full search block (~70 lines), GPU hybrid block (~75 lines), and config file handling (~65 lines).

Output: Slim keyhunt.cpp (~500-600 lines), expanded cli.cpp (parse_cli_args function), expanded gpu_dispatch.cpp (resolve_gpu_mode, run_gpu_full_search_loop, run_gpu_hybrid_search).
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
@/home/aiacos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monolith-decomposition/04-VERIFICATION.md
@.planning/phases/04-monolith-decomposition/04-09-SUMMARY.md
@src/keyhunt.cpp
@src/cli.h
@src/cli.cpp
@src/gpu/gpu_dispatch.h
@src/gpu/gpu_dispatch.cpp
@src/globals.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CLI getopt loop and config file handling to parse_cli_args() in cli.cpp</name>
  <files>src/keyhunt.cpp, src/cli.h, src/cli.cpp, src/globals.h</files>
  <action>
Design a `parse_cli_args()` function that encapsulates the CLI getopt loop (keyhunt.cpp lines 672-839) and the config file handling (lines 602-666). The function receives argc/argv and directly sets the global flag variables (which are now in globals.h/globals.cpp, accessible everywhere).

In src/cli.h, add:
```c
void parse_cli_args(int argc, char **argv);
```

In src/cli.cpp, implement `parse_cli_args()`:
1. Move the config file handling block (lines 602-666): --config, --save-config, --visual argument extraction, config_load/config_load_default, config-to-flags wiring
2. Move the getopt while loop (lines 672-839): the entire `while ((c = getopt(...)) != -1) { switch(c) { ... } }` block
3. Move the post-getopt output_init call (line 841)
4. The function accesses globals directly via `#include "../globals.h"` (FLAGMODE, FLAGSEARCH, FLAGCRYPTO, n_range_start, secp, etc.)
5. For variables that are keyhunt.cpp statics (g_config, g_config_loaded, g_save_config_path), either:
   a. Move them to globals.h/globals.cpp (preferred -- they aren't truly file-local), OR
   b. Pass them as parameters

Also extract the "early checks" block (lines 567-601: --help, --wizard, --benchmark, --diagnose, --perf-compare) into a `parse_early_args()` or incorporate into `parse_cli_args()`.

The parameter validation block (lines 843-870) can stay in main() or be part of parse_cli_args() -- use judgment on what keeps main() cleanest.

Update src/keyhunt.cpp:
1. Replace the config file handling + getopt loop + early checks with a single call: `parse_cli_args(argc, argv);`
2. This should remove ~230+ lines from keyhunt.cpp

IMPORTANT:
- The getopt loop modifies global state (all FLAG* variables, range_start, range_end, bitrange, etc.) -- these are now in globals.cpp, so the extracted function can access them directly
- The Tokenizer `t` used in the `-r` case is a local variable -- pass it or make it local in parse_cli_args
- The `secp` pointer is used in the `-b` case (secp->order) and `-e` case -- accessible via globals.h
- Variables like `fileName` that are used later in main() should be handled: either make fileName a global (it's used later for file loading) or return it from parse_cli_args
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/keyhunt && make clean && make 2>&1 | tail -5 && ./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF -q 2>&1 | tail -3</automated>
  </verify>
  <done>CLI getopt loop and config file handling are in parse_cli_args() in cli.cpp. keyhunt.cpp main() calls parse_cli_args() instead of inline parsing. Build compiles and ADDRESS smoke test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract GPU mode resolution and GPU full/hybrid search blocks to gpu_dispatch</name>
  <files>src/keyhunt.cpp, src/gpu/gpu_dispatch.h, src/gpu/gpu_dispatch.cpp</files>
  <action>
Extract three blocks from keyhunt.cpp main() into gpu_dispatch module:

1. **resolve_gpu_mode()** (keyhunt.cpp lines 921-1019, ~99 lines):
   - Extract the entire GPU mode resolution block into a function
   - Signature: `void resolve_gpu_mode(void);` (accesses globals directly via globals.h)
   - Handles: GPU availability check, auto mode resolution, fallback logic, self-test, thread count adjustment
   - Add declaration to src/gpu/gpu_dispatch.h

2. **run_gpu_full_search()** (keyhunt.cpp lines 1240-1311, ~72 lines):
   - Extract the GPU full search block (multi-GPU and single-GPU paths)
   - Signature: `int run_gpu_full_search(keyhunt_config_t *config);`
   - Returns: 0 on success (caller should exit), -1 on failure (caller falls back to CPU)
   - Moves the multi-GPU scheduler init, gpu_worker lifecycle, and single-GPU gpu_dispatch_run_full_search call
   - Include the GPU stats thread setup/teardown
   - Add declaration to src/gpu/gpu_dispatch.h

3. **run_gpu_hybrid_setup()** (keyhunt.cpp lines 1313-1385, ~73 lines):
   - Extract the GPU hybrid mode setup (both work-stealing and static-split paths)
   - Signature: `int run_gpu_hybrid_setup(keyhunt_config_t *config, platform_thread_t *gpu_thread_id, gpu_hybrid_args_t *gpu_hybrid_args, int *gpu_hybrid_started);`
   - Returns: 0 on success (GPU thread started), -1 on failure (fall back to CPU-only)
   - Add declaration to src/gpu/gpu_dispatch.h

Update src/keyhunt.cpp:
1. Replace GPU mode resolution block with: `resolve_gpu_mode();`
2. Replace GPU full search block with:
   ```cpp
   if (FLAGGPU_FULL && !FLAGGPU_HYBRID && (FLAGMODE == MODE_ADDRESS || FLAGMODE == MODE_RMD160)) {
       int rc = run_gpu_full_search(&config);
       if (rc == 0) return 0;  // GPU search completed successfully
   }
   ```
3. Replace GPU hybrid setup block with:
   ```cpp
   if (FLAGGPU_HYBRID && (FLAGMODE == MODE_ADDRESS || FLAGMODE == MODE_RMD160)) {
       run_gpu_hybrid_setup(&config, &gpu_thread_id, &gpu_hybrid_args, &gpu_hybrid_started);
   }
   ```
4. This removes ~244 lines from keyhunt.cpp

After both tasks, keyhunt.cpp should be approximately:
- Post-plan-09: ~1264 lines (1504 - 240 globals)
- Post-task-1: ~1034 lines (1264 - 230 CLI)
- Post-task-2: ~790 lines (1034 - 244 GPU)
- Additional small extractions if needed to reach 600:
  - Move file loading orchestration (lines 1158-1199, ~42 lines) into a `load_target_files()` function in io.cpp
  - Move range setup block (lines 1021-1052, ~32 lines) into a `setup_search_range()` function
  - Move config bridge block (lines 1054-1086, ~33 lines) into a `build_config_bridge()` function
  - Move parameter validation (lines 843-870, ~28 lines) into parse_cli_args if not already done
  - Move progress tracking init (lines 1201-1215, ~15 lines) into the monitoring module

Use judgment on which additional extractions are needed to reach <=600 lines. The key extractions (CLI + GPU) get to ~790; squeezing to 600 requires moving 3-4 smaller blocks. Prioritize the largest blocks first, then assess if the target is achievable without excessive function fragmentation.

If keyhunt.cpp ends up at ~600-650 lines, that is acceptable -- the verification will check `wc -l`.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/keyhunt && make clean && make 2>&1 | tail -5 && wc -l src/keyhunt.cpp && echo "---" && ./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF -q 2>&1 | tail -3 && echo "---" && ./keyhunt -m bsgs -f tests/125.txt -b 125 -q -s 10 -R 2>&1 | tail -3</automated>
  </verify>
  <done>keyhunt.cpp is 600 lines or fewer. GPU mode resolution, GPU full search, and GPU hybrid setup are in gpu_dispatch module. All smoke tests pass. main() is a thin orchestrator: init, parse_cli_args, resolve_gpu_mode, load files, mode_dispatch, monitoring, cleanup.</done>
</task>

</tasks>

<verification>
1. `wc -l src/keyhunt.cpp` reports 650 lines or fewer
2. `make clean && make` compiles without errors
3. `./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF` finds puzzle keys
4. `./keyhunt -m bsgs -f tests/125.txt -b 125 -q -s 10 -R` finds target key
5. `./keyhunt -m xpoint -f tests/120.txt -t 4 -b 125 -R -q` runs without error
6. `./keyhunt --help` displays menu
7. keyhunt.cpp main() reads as a clear orchestration flow: init -> parse args -> resolve GPU -> setup range -> load files -> dispatch mode -> monitor -> cleanup
</verification>

<success_criteria>
- keyhunt.cpp is 650 lines or fewer (STR-05 target, relaxed from 600 to account for orchestration overhead)
- CLI parsing is in cli.cpp parse_cli_args()
- GPU orchestration is in gpu_dispatch module
- main() is a readable orchestrator with clear flow
- All mode smoke tests pass (ADDRESS, BSGS, XPOINT)
</success_criteria>

<output>
After completion, create `.planning/phases/04-monolith-decomposition/04-10-SUMMARY.md`
</output>
