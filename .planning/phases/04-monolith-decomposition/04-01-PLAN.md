---
phase: 04-monolith-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/io/io.h
  - src/io/io.cpp
  - src/util/thread_util.h
  - src/util/thread_util.cpp
  - src/util/profiling.h
  - src/util/profiling.cpp
  - src/util/work_queue.h
  - src/util/work_queue.cpp
  - src/keyhunt.cpp
  - Makefile
autonomous: true
requirements: [STR-05]

must_haves:
  truths:
    - "io.cpp writekey/writekeyeth accept const keyhunt_config_t* and use no extern globals"
    - "Utility functions (thread_rand, sleep_ms, profiling, work_queue) compile from src/util/ without referencing keyhunt.cpp"
    - "All existing tests still pass after extraction"
  artifacts:
    - path: "src/io/io.h"
      provides: "Updated writekey/writekeyeth signatures with config parameter"
      contains: "writekey(const keyhunt_config_t"
    - path: "src/io/io.cpp"
      provides: "Config-wired io with zero local extern declarations"
    - path: "src/util/thread_util.h"
      provides: "thread_rand, sleep_ms, aligned_calloc, aligned_free declarations"
      exports: ["thread_rand", "sleep_ms", "aligned_calloc", "aligned_free"]
    - path: "src/util/profiling.h"
      provides: "Profiling function declarations"
      exports: ["profile_init_threads", "profile_set_thread", "append_profile_info"]
    - path: "src/util/work_queue.h"
      provides: "Work queue and WorkPool declarations"
  key_links:
    - from: "src/io/io.cpp"
      to: "src/config/config.h"
      via: "config parameter to writekey/writekeyeth"
      pattern: "config->runtime\\.secp|config->search\\.mode"
    - from: "src/util/thread_util.cpp"
      to: "src/keyhunt.cpp"
      via: "no dependency (extracted cleanly)"
---

<objective>
Wire io.cpp through config (completing CFG-07) and extract utility functions from keyhunt.cpp to src/util/, eliminating circular dependencies before mode extraction.

Purpose: io.cpp's 22 local externs are the last remaining config migration debt. Utility functions (thread_rand, sleep_ms, profiling, work_queue) must be extracted first per Research Pitfall 6 -- modes will depend on util, not on keyhunt.cpp.
Output: Config-wired io.cpp, src/util/ with extracted utilities, updated Makefile
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
@/home/aiacos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monolith-decomposition/04-CONTEXT.md
@.planning/phases/04-monolith-decomposition/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/config/config.h:
```c
typedef struct { ... } keyhunt_config_t;
// runtime_state_t fields used by io.cpp:
//   void *secp;           // Secp256K1*
//   void *write_mutex;    // platform_mutex_t*
//   void *address_table;  // address_value*
//   int64_t address_count;
//   void *bloom_filter;   // bloom_extended_t*
// search_config_t fields:
//   search_mode_t mode;
//   crypto_type_t crypto_type;
//   key_format_t key_format;
//   bool quiet_mode;
```

From src/io/io.h (current signatures to change):
```cpp
void writekey(bool compressed, Int *key);
void writekeyeth(Int *key);
```

From src/search/search_common.h:
```cpp
struct thread_args {
    keyhunt_config_t *config;
    int thread_id;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire io.cpp through config and update all call sites</name>
  <files>src/io/io.h, src/io/io.cpp, src/search/search_address.cpp, src/search/search_vanity.cpp, src/search/search_minikeys.cpp, src/search/search_bsgs_threads.cpp, src/keyhunt.cpp</files>
  <action>
1. Update src/io/io.h: Change writekey signature to `void writekey(const keyhunt_config_t *config, bool compressed, Int *key)` and writekeyeth to `void writekeyeth(const keyhunt_config_t *config, Int *key)`. Add `#include "../config/config.h"` if not present. Also update writevanitykey if it exists in io.h.

2. Update src/io/io.cpp: Remove all 22 local extern declarations. Replace each with reads from the config parameter:
   - `extern Secp256K1 *secp` -> `Secp256K1 *secp = (Secp256K1 *)config->runtime.secp;`
   - `extern int FLAGMODE` -> `int FLAGMODE = (int)config->search.mode;`
   - `extern int FLAGCRYPTO` -> `int FLAGCRYPTO = (int)config->search.crypto_type;`
   - `extern int FLAGSEARCH` -> `int FLAGSEARCH = (int)config->search.key_format;`
   - `extern platform_mutex_t write_keys` -> `platform_mutex_t *write_mutex = (platform_mutex_t *)config->runtime.write_mutex;`
   - `extern int64_t N` -> `int64_t N = config->runtime.address_count;`
   - `extern struct address_value *addressTable` -> `struct address_value *addressTable = (struct address_value *)config->runtime.address_table;`
   - Continue for all 22 externs, using local shadow variables matching old names to minimize code churn inside function bodies.
   - For mutex operations, change `platform_mutex_lock(&write_keys)` to `platform_mutex_lock(write_mutex)` (pointer not address-of).

3. Update all call sites in search modules (these already have config pointer from Phase 3):
   - src/search/search_address.cpp: `writekey(config, compressed, &key)` (already has config from thread_args)
   - src/search/search_vanity.cpp: `writekey(config, compressed, &key)` and `writevanitykey(config, ...)`
   - src/search/search_minikeys.cpp: `writekey(config, compressed, &key)`
   - src/search/search_bsgs_threads.cpp: `writekey(config, compressed, &key)`
   - src/keyhunt.cpp: Any direct writekey calls pass `&config`

4. Verify all 22 externs are gone from io.cpp: `grep -n "extern" src/io/io.cpp` should return zero matches (or only legitimate extern "C" linkage).
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/keyhunt && make clean all 2>&1 | tail -5 && grep -c "^extern " src/io/io.cpp; echo "exit: $?"</automated>
  </verify>
  <done>io.cpp has zero local extern declarations. writekey/writekeyeth accept config parameter. All search modules pass config to io functions. Build succeeds. Quick smoke test passes: `./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF`</done>
</task>

<task type="auto">
  <name>Task 2: Extract utility functions from keyhunt.cpp to src/util/</name>
  <files>src/util/thread_util.h, src/util/thread_util.cpp, src/util/profiling.h, src/util/profiling.cpp, src/util/work_queue.h, src/util/work_queue.cpp, src/keyhunt.cpp, Makefile</files>
  <action>
1. Create src/util/ directory.

2. Create src/util/thread_util.h and src/util/thread_util.cpp:
   - Move from keyhunt.cpp: `aligned_calloc()` (lines 97-112), `aligned_free()` (lines 114-125), `thread_rand_init()`, `thread_rand()`, `thread_rand_n()` (lines 146-176), `sleep_ms()` (line 578).
   - Include guards, appropriate headers (stdint.h, stdbool.h, stddef.h).
   - For thread_rand: keep the thread_local state variables with the functions.

3. Create src/util/profiling.h and src/util/profiling.cpp:
   - Move from keyhunt.cpp: `g_profile_enabled` (line 234), `profile_counters_t` related code, `profile_now_ns()` (line 241), `profile_init_threads()` (line 262), `profile_set_thread()` (line 286), `profile_aggregate()` (line 294), `append_profile_info()` (line 307), `append_adaptive_info()` (line 356).
   - `g_profile_enabled` becomes an extern in profiling.h, defined in profiling.cpp.
   - Keep `profile_set_thread()` non-static since search modules call it via extern.

4. Create src/util/work_queue.h and src/util/work_queue.cpp:
   - Move from keyhunt.cpp: `WorkPool` struct (lines 391-490), `g_work_pool` global, `configure_work_queue()` (line 573), `shutdown_work_queue()` (line 1107), `acquire_base_key()` (line 576).
   - The WorkQueue template is from core/workqueue.h -- just move the usage/global instance.

5. Update keyhunt.cpp: Replace moved code with `#include "util/thread_util.h"`, `#include "util/profiling.h"`, `#include "util/work_queue.h"`. Remove the moved function bodies and global definitions. Keep forward declarations or extern references as needed.

6. Update Makefile: Add src/util/thread_util.o, src/util/profiling.o, src/util/work_queue.o to OBJS list. Add compilation rules with appropriate flags.

7. Verify build and tests pass.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/keyhunt && make clean all 2>&1 | tail -5 && ./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF 2>&1 | grep -c "KEY FOUND" && echo "Smoke test OK"</automated>
  </verify>
  <done>src/util/ directory exists with thread_util, profiling, and work_queue modules. keyhunt.cpp no longer defines these functions. Build succeeds. All E2E mode tests pass.</done>
</task>

</tasks>

<verification>
- `make clean all` succeeds with zero errors
- `grep -c "^extern " src/io/io.cpp` returns 0
- `./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF` finds expected keys
- `make test` passes (unit tests)
</verification>

<success_criteria>
- io.cpp has zero local extern declarations (CFG-07 complete)
- src/util/ contains thread_util, profiling, work_queue modules
- All moved functions compile independently from keyhunt.cpp
- No regressions in test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-monolith-decomposition/04-01-SUMMARY.md`
</output>
