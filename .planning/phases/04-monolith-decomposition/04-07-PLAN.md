---
phase: 04-monolith-decomposition
plan: 07
type: execute
wave: 1
depends_on: ["04-04", "04-05"]
files_modified:
  - src/modes/mode_bsgs.cpp
  - src/modes/bsgs_globals.h
  - src/keyhunt.cpp
  - src/io/io.cpp
  - src/io/io.h
autonomous: true
gap_closure: true
requirements: [STR-02, STR-05]

must_haves:
  truths:
    - "mode_bsgs.cpp compiles without extern declarations to keyhunt.cpp globals"
    - "BSGS smoke test passes: ./keyhunt -m bsgs -f tests/125.txt -b 125 -q -s 10 -R finds expected key"
    - "io.cpp readFile* functions accept config parameter and use no extern globals"
  artifacts:
    - path: "src/modes/bsgs_globals.h"
      provides: "BSGS state declarations owned by mode_bsgs.cpp"
      min_lines: 60
    - path: "src/modes/mode_bsgs.cpp"
      provides: "BSGS mode with owned globals (no keyhunt.cpp extern bridge)"
    - path: "src/io/io.cpp"
      provides: "readFileAddress/readFileVanity accepting config parameter"
  key_links:
    - from: "src/modes/mode_bsgs.cpp"
      to: "src/modes/bsgs_globals.h"
      via: "include for owned BSGS state"
      pattern: '#include "bsgs_globals.h"'
    - from: "src/io/io.cpp"
      to: "src/config/config.h"
      via: "config parameter in readFile functions"
      pattern: "keyhunt_config_t"
---

<objective>
Eliminate the extern bridge between mode_bsgs.cpp and keyhunt.cpp, and wire io.cpp readFile* functions through config.

Purpose: mode_bsgs.cpp currently has 108 extern declarations referencing keyhunt.cpp globals. This plan moves BSGS-specific globals (Int/Point BSGS_*, bloom_bP*, bPtable, bsgs_found, etc.) from keyhunt.cpp definitions into mode_bsgs.cpp ownership via a bsgs_globals.h header. Shared state (secp, config fields, range vars) is accessed through the config pointer. io.cpp's 43 extern declarations are also eliminated by wiring readFile* through config.

Output: mode_bsgs.cpp with zero extern declarations to keyhunt.cpp; io.cpp readFile* with config parameter.
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
@/home/aiacos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monolith-decomposition/04-CONTEXT.md
@.planning/phases/04-monolith-decomposition/04-VERIFICATION.md
@.planning/phases/04-monolith-decomposition/04-04-SUMMARY.md
@src/modes/mode_bsgs.cpp
@src/keyhunt.cpp
@src/config/config.h
@src/io/io.cpp
@src/io/io.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move BSGS globals from keyhunt.cpp to mode_bsgs.cpp ownership</name>
  <files>src/modes/bsgs_globals.h, src/modes/mode_bsgs.cpp, src/keyhunt.cpp</files>
  <action>
Create `src/modes/bsgs_globals.h` as the authoritative declaration header for all BSGS-specific state. This header is included by mode_bsgs.cpp (which DEFINES the variables) and by keyhunt.cpp (which extern-references the few it still needs for cleanup).

**Variables to move from keyhunt.cpp to mode_bsgs.cpp (become file-scope definitions in mode_bsgs.cpp):**

1. BSGS Int variables (keyhunt.cpp lines 564-579): BSGS_GROUP_SIZE, BSGS_CURRENT, BSGS_R, BSGS_AUX, BSGS_N, BSGS_N_double, BSGS_M, BSGS_M_double, BSGS_M2, BSGS_M2_double, BSGS_M3, BSGS_M3_double, ONE, ZERO, MPZAUX
2. BSGS Point variables (lines 581-594): BSGS_P, BSGS_MP, BSGS_MP2, BSGS_MP3, BSGS_MP_double, BSGS_MP2_double, BSGS_MP3_double, BSGS_AMP2, BSGS_AMP3, point_temp, point_temp2
3. BSGS bloom/state (lines 489-500, 522-544): BSGS_XVALUE_RAM, BSGS_BUFFERXPOINTLENGTH, BSGS_BUFFERREGISTERLENGTH, bloom_bP_totalbytes, bloom_bP2/3_totalbytes, bsgs_m/m2/m3/aux, bsgs_point_number, bsgs_found, OriginalPointsBSGS, OriginalPointsBSGScompressed, bPtable, bloom_bP, bloom_bPx2nd, bloom_bPx3rd, bloom_bP_checksums, bloom_bPx2nd_checksums, bloom_bPx3rd_checksums, bloom_bP_mutex, bloom_bPx2nd_mutex, bloom_bPx3rd_mutex, bPload_mutex
4. BSGS file state (lines 527-529): bytes, checksum[32], checksum_backup[32], buffer_bloom_file[1024]
5. BSGS string arrays: bsgs_modes[5]
6. BSGS thread functions: thread_bPload, thread_bPload_2blooms declarations
7. BSGS cleanup: cleanup_bsgs_resources() (lines 671-755) moves to mode_bsgs.cpp

**Variables that STAY in keyhunt.cpp (shared across modes):**
- secp, Gn, _2Gn, GSn, _2GSn (generator points -- used by all modes)
- n_range_start, n_range_end, n_range_diff, n_range_aux, stride (range -- used by all modes)
- N, addressTable, bloom (target data -- used by io.cpp/all modes)
- steps, ends, tid (thread management -- used by monitoring loop)
- THREADOUTPUT, FINISHED_ITEMS, FINISHED_THREADS_COUNTER, etc. (counters)
- All FLAG* variables (set by CLI parser)
- write_keys, write_random, bsgs_thread (mutexes -- used by orchestrator)
- lambda, lambda2, beta, beta2 (endomorphism constants)

**In mode_bsgs.cpp:**
- Remove ALL 108 `extern` declarations at the top
- Add `#include "bsgs_globals.h"`
- For shared state that mode_bsgs.cpp needs from keyhunt.cpp (secp, N, addressTable, flags, ranges, counters), access through keyhunt_config_t* parameter OR keep a minimal set of extern declarations only for the truly shared globals that are also used by other modes (secp, N, addressTable, FLAG*, range vars, counters, mutexes)
- The goal is: NO extern to any BSGS-specific global. Shared globals accessed via config where possible, remaining shared externs (secp, FLAG*, counters) are acceptable since they are shared state, not BSGS-owned state.

**In keyhunt.cpp:**
- Delete all BSGS-specific variable definitions (moved to mode_bsgs.cpp)
- Delete cleanup_bsgs_resources() body (moved to mode_bsgs.cpp, keyhunt.cpp calls via mode ops cleanup)
- Keep `extern` declarations for the few BSGS globals keyhunt.cpp still needs (if any -- likely none since BSGS state is self-contained)
- Update atexit() to call mode cleanup instead of inline cleanup

**Important constraints:**
- bsgs_context_t is allocated at main() scope (line 1344) -- keep the allocation there, pass via config.runtime.bsgs_context
- bPload threads run during BSGS init -- they now access BSGS globals directly in mode_bsgs.cpp (same translation unit, no externs needed)
- ONE, ZERO, MPZAUX are also used by monitoring loop Int arithmetic -- either keep in keyhunt.cpp or move and extern from monitoring code
- BSGS_GROUP_SIZE is set from CPU_GRP_SIZE in main() before mode dispatch -- pass via config or set in mode_bsgs_init

After changes, count remaining extern declarations in mode_bsgs.cpp. Target: fewer than 30 (shared state only, no BSGS-owned state).
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/keyhunt && make clean all 2>&1 | tail -5 && grep -c "^extern" src/modes/mode_bsgs.cpp && echo "---" && ./keyhunt -m bsgs -f tests/125.txt -b 125 -q -s 10 -R 2>&1 | head -20</automated>
  </verify>
  <done>mode_bsgs.cpp has fewer than 30 extern declarations (down from 108), all BSGS-specific globals defined in mode_bsgs.cpp, BSGS smoke test passes, build is clean</done>
</task>

<task type="auto">
  <name>Task 2: Wire io.cpp readFile* functions through config, eliminate externs</name>
  <files>src/io/io.h, src/io/io.cpp, src/keyhunt.cpp</files>
  <action>
Change signatures of readFileAddress and readFileVanity to accept `keyhunt_config_t *config`:

```cpp
bool readFileAddress(const char *fileName, keyhunt_config_t *config);
bool readFileVanity(const char *fileName, keyhunt_config_t *config);
bool writeFileIfNeeded(const char *fileName, keyhunt_config_t *config);
```

In each function body, replace the function-local extern declarations with config reads:
- `extern int FLAGMODE` -> `config->search.mode`
- `extern int FLAGCRYPTO` -> `(int)config->search.crypto_type`
- `extern int FLAGSAVEREADFILE` -> use a new bool in search_config_t or pass as parameter
- `extern int FLAGSKIPCHECKSUM` -> `config->search.skip_checksum`
- `extern int FLAGVANITY` -> derive from `config->search.mode == MODE_VANITY`
- `extern int FLAGREADEDFILE1` -> use a new flag in runtime_state_t or return value
- `extern int MAXLENGTHADDRESS` -> `config->runtime.max_address_length`
- `extern uint64_t N` -> `config->runtime.address_count` (cast from int64_t)
- `extern bloom_extended_t bloom` -> `(bloom_extended_t *)config->runtime.bloom_filter` (need to store by pointer)
- `extern struct address_value *addressTable` -> `(struct address_value *)config->runtime.address_table`
- `extern int vanity_rmd_targets` -> `config->runtime.vanity_targets`
- `extern int vanity_rmd_total` -> `config->runtime.vanity_total`
- `extern int *vanity_rmd_limits` -> `(int *)config->runtime.vanity_limits`
- `extern uint8_t ***vanity_rmd_limit_values_A` -> `(uint8_t ***)config->runtime.vanity_values_a`
- `extern int vanity_rmd_minimun_bytes_check_length` -> `config->runtime.vanity_min_check_len`
- `extern struct bloom *vanity_bloom` -> `(struct bloom *)config->runtime.vanity_bloom`
- `extern int FLAGDEBUG` -> `config->search.debug_mode`

For variables that readFileAddress WRITES (N, addressTable, bloom -- these are output parameters), the function must write back through config pointers. Since config->runtime.address_count is int64_t and N is uint64_t, cast appropriately. addressTable and bloom are void* in config -- the function allocates and stores back.

Key pattern: readFileAddress populates N, addressTable, and bloom. After the call returns, keyhunt.cpp must update its local copies (N, addressTable, bloom) from config if it still uses them directly. Alternatively, make keyhunt.cpp always read from config for these values after readFile returns.

For FLAGSAVEREADFILE, FLAGREADEDFILE1 etc.: these are file-caching state flags. Add `bool save_read_file` and `bool read_cached_file[4]` to search_config_t or runtime_state_t. Wire them in the config bridge in keyhunt.cpp (lines ~2800-2900).

Update call sites in keyhunt.cpp (around lines 2421, 2427) to pass &config.

After changes, verify: `grep -c "extern" src/io/io.cpp` should be 0.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/keyhunt && make clean all 2>&1 | tail -5 && grep -c "extern" src/io/io.cpp && echo "---" && ./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF 2>&1 | head -20</automated>
  </verify>
  <done>io.cpp has zero extern declarations, readFileAddress/readFileVanity accept config parameter, ADDRESS mode smoke test passes</done>
</task>

</tasks>

<verification>
1. `make clean all` succeeds with no errors
2. `grep -c "^extern" src/modes/mode_bsgs.cpp` shows fewer than 30 (down from 108)
3. `grep -c "extern" src/io/io.cpp` shows 0
4. `./keyhunt -m bsgs -f tests/125.txt -b 125 -q -s 10 -R` finds expected key
5. `./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF` finds expected keys
6. `make clang-tidy` exits 0 (no regressions)
</verification>

<success_criteria>
- mode_bsgs.cpp extern count reduced from 108 to fewer than 30
- io.cpp extern count reduced from 43 to 0
- All BSGS-specific globals owned by mode_bsgs.cpp, not keyhunt.cpp
- Both BSGS and ADDRESS smoke tests pass
- Build compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-monolith-decomposition/04-07-SUMMARY.md`
</output>
