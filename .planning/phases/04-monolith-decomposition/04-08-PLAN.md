---
phase: 04-monolith-decomposition
plan: 08
type: execute
wave: 2
depends_on: ["04-07"]
files_modified:
  - src/keyhunt.cpp
  - src/monitoring/monitoring.h
  - src/monitoring/monitoring.cpp
  - src/gpu/gpu_dispatch.h
  - src/gpu/gpu_dispatch.cpp
  - src/cli.h
  - src/cli.cpp
  - Makefile
autonomous: true
gap_closure: true
requirements: [STR-05, STR-01, STR-03, STR-04]

must_haves:
  truths:
    - "keyhunt.cpp is 600 lines or fewer"
    - "All search modes still pass smoke tests"
    - "Monitoring loop correctly reports progress when run externally"
  artifacts:
    - path: "src/monitoring/monitoring.h"
      provides: "Monitoring loop API declaration"
      min_lines: 15
    - path: "src/monitoring/monitoring.cpp"
      provides: "Monitoring loop implementation extracted from keyhunt.cpp"
      min_lines: 200
    - path: "src/keyhunt.cpp"
      provides: "Thin orchestrator"
  key_links:
    - from: "src/keyhunt.cpp"
      to: "src/monitoring/monitoring.h"
      via: "run_monitoring_loop() call"
      pattern: "run_monitoring_loop"
    - from: "src/keyhunt.cpp"
      to: "src/modes/modes.h"
      via: "mode_dispatch() call"
      pattern: "mode_dispatch"
---

<objective>
Reduce keyhunt.cpp from ~3000+ lines (after plan 04-07) to 600 lines or fewer by extracting the monitoring loop, GPU full/hybrid dispatch logic, menu(), init_generator(), and remaining utility statics into proper modules.

Purpose: This is the final slimming step to achieve STR-05 (keyhunt.cpp <= 600 lines). After plan 04-07 removes ~300 lines of BSGS globals, this plan extracts the remaining large blocks: monitoring loop (~456 lines), GPU full/hybrid search dispatch (~381 lines), GPU mode resolution (~200 lines), utility statics, and menu/init_generator.

Output: keyhunt.cpp as a thin orchestrator containing only main(), CLI parsing, config building, mode_dispatch() calls, and a call to run_monitoring_loop().
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
@/home/aiacos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monolith-decomposition/04-CONTEXT.md
@.planning/phases/04-monolith-decomposition/04-VERIFICATION.md
@src/keyhunt.cpp
@src/gpu/gpu_dispatch.h
@src/gpu/gpu_dispatch.cpp
@src/modes/modes.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract monitoring loop and GPU dispatch logic from keyhunt.cpp</name>
  <files>src/monitoring/monitoring.h, src/monitoring/monitoring.cpp, src/gpu/gpu_dispatch.h, src/gpu/gpu_dispatch.cpp, src/keyhunt.cpp, Makefile</files>
  <action>
**Create src/monitoring/ directory and monitoring module:**

Create `src/monitoring/monitoring.h` with:
```cpp
#ifndef KEYHUNT_MONITORING_H
#define KEYHUNT_MONITORING_H
#include "../config/config.h"
// Run the main monitoring loop that prints status, tracks progress, and waits for thread completion.
// Parameters are all the state needed from the orchestrator.
int run_monitoring_loop(keyhunt_config_t *config, int num_threads,
                        struct thread_counter *steps, struct thread_flag *ends,
                        int flagmode, int gpu_hybrid_started,
                        /* additional params as needed */);
#endif
```

Create `src/monitoring/monitoring.cpp` by extracting from keyhunt.cpp:
1. The `do { ... } while(continue_flag)` monitoring loop (lines ~2920-3331) -- this is the core status printing, progress tracking, GPU hybrid stats aggregation
2. The GPU hybrid cleanup section (lines ~3333-3360)
3. The progress completion section (lines ~3362-3366)
4. Supporting static functions that ONLY the monitoring loop uses:
   - `format_keys_per_second()` (lines 606-640)
   - `append_progress_info()` (lines 999-1065)
   - `append_adaptive_info()` (lines 164-197)
   - `capture_progress_metrics()` (lines 895-997)
   - `snapshot_range_next_key()` (lines 885-893)
   - `format_hex_position()` (lines 867-883)
   - `initialize_rate_limits()` and `int_limits[7]`, `str_limits_prefixs[7]`, `str_limits[7]` (lines 546-560)
   - `span_u64_from_range()` (lines 642-659)
   - `gpu_full_stats_thread()` and `gpu_full_stats_args_t` (lines 1067-1135)
   - `gpu_keys_checked_total_u64()` (lines 469-483)
   - `initialize_range_progress_tracker()` (lines 842-865)
   - `g_rangeProgressEnabled`, `g_rangeProgressStart`, `g_rangeProgressEnd`, `g_rangeProgressSpan` (lines 601-604)
   - Progress state: `g_progress_state`, `g_progress_enabled` (lines 134-135)

The monitoring function receives all needed state through parameters and config pointer. For globals it reads (OUTPUTSECONDS, FLAGMATRIX, FLAGQUIET, FLAGVISUAL, FLAGGPU_HYBRID, FLAGMODE, etc.), either pass as parameters or read from config.

**Move GPU full/hybrid search dispatch to gpu_dispatch.cpp:**

The GPU full search mode block (lines ~2540-2900) contains:
- GPU full search initialization (reset stats, stats thread, single/multi-GPU dispatch)
- GPU hybrid mode setup (split range, launch GPU thread, adaptive scheduler init)
- Multi-GPU worker init with signal handler

This entire block should move to a new function in gpu_dispatch.cpp:
```cpp
int gpu_run_full_search(keyhunt_config_t *config, ...);
int gpu_setup_hybrid(keyhunt_config_t *config, platform_thread_t *gpu_thread_id, gpu_hybrid_args_t *args, ...);
```

keyhunt.cpp calls these functions instead of having the inline GPU logic.

Also move to gpu_dispatch.cpp:
- `hybrid_get_gpu_range_percent_default()` (lines 502-520)
- `gpu_selftest_hash160_fromX()` (lines 1189-1250)

**Move menu() to cli.cpp:**
- menu() (lines 3392-3472, ~80 lines) is a help screen. Move to src/cli.cpp (which already exists for CLI type enums). Add `void menu()` declaration to cli.h.

**Move init_generator() to modes or util:**
- init_generator() (lines 3377-3390, ~14 lines) initializes Gn and _2Gn generator points. It uses secp, stride, CPU_GRP_SIZE. Move to a utility file or keep inline since it is very small (discretion).

**Move remaining utility statics:**
- `cpu_use_y_parity_for_compressed_btc()` (lines 434-467) -- move to gpu_dispatch.cpp or a search utility
- `cleanup_general_resources()` (lines 661-669) -- keep in keyhunt.cpp (short)
- `flagsearch_to_key_format()` (lines 111-128) -- move to cli.cpp
- `get_mode_name()` (lines 272-283) -- move to cli.cpp
- `configure_work_queue()` / `shutdown_work_queue()` (already in util)
- `acquire_base_key()` (lines 1144-1187) -- move to util/work_queue.cpp or keep (it uses g_workQueue)
- `env_truthy_kh()` (lines 145-160) -- move to util

**Move remaining global variable definitions that are only used by monitoring:**
- OUTPUTSECONDS (Int) -- move to monitoring.cpp
- int_limits[7] -- move to monitoring.cpp
- str_limits_prefixs, str_limits -- move to monitoring.cpp

**Move global variable definitions that are only used by BSGS (if any remain after 04-07).**

**Move struct definitions that are mode-specific:**
- `struct checksumsha256` (line 199) -- used by BSGS only, move to bsgs_globals.h
- `struct bPload` (line 214) -- BSGS only, move to bsgs_globals.h
- `struct publickey` (lines 226-243) -- used by search, already in search headers? If not, move

**Update Makefile:**
- Add src/monitoring/monitoring.o to OBJS
- Ensure src/monitoring/ is in include paths for clang-tidy

**After extraction, keyhunt.cpp should contain approximately:**
- Includes (~80 lines)
- Shared global definitions that multiple modules need: secp, Gn, _2Gn, GSn, _2GSn, n_range_*, stride, N, addressTable, bloom, FLAG*, counters, mutexes (~150 lines)
- main() function with: init, CLI getopt loop, parameter validation, config bridge, mode_dispatch calls, monitoring loop call, cleanup (~350 lines)
- Total: ~580 lines

If still over 600, look for:
- Move FLAG* definitions to a dedicated flags module
- Consolidate config bridge into a helper function
- Move more variable definitions to their owning mode files
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/keyhunt && make clean all 2>&1 | tail -5 && wc -l src/keyhunt.cpp && echo "---" && ./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF 2>&1 | head -10 && echo "---" && ./keyhunt -m bsgs -f tests/125.txt -b 125 -q -s 10 -R 2>&1 | head -10</automated>
  </verify>
  <done>keyhunt.cpp is 600 lines or fewer, all search modes pass smoke tests, monitoring loop works correctly from extracted module, build is clean</done>
</task>

<task type="auto">
  <name>Task 2: Verify all modes and static analysis gates</name>
  <files>src/keyhunt.cpp</files>
  <action>
Run comprehensive verification:

1. Build: `make clean all` -- must succeed with zero errors
2. Line count: `wc -l src/keyhunt.cpp` -- must be <= 600
3. Extern audit: `grep -c "^extern" src/modes/mode_bsgs.cpp` -- must be < 30
4. Extern audit: `grep -c "extern" src/io/io.cpp` -- must be 0
5. Smoke tests for all modes:
   - `./keyhunt -m address -f tests/1to32.txt -r 1:FFFFFFFF`
   - `./keyhunt -m bsgs -f tests/125.txt -b 125 -q -s 10 -R`
   - `./keyhunt -m xpoint -f tests/120.txt -t 4 -b 125 -R -q`
   - `./keyhunt -m rmd160 -f tests/66.rmd -b 66 -l compress -R -q`
6. Static analysis: `make clang-tidy` -- must exit 0
7. Static analysis: `make cppcheck` -- must exit 0

If any test fails, fix the issue. Common problems:
- Missing extern for shared globals in new modules -- add minimal externs or pass via config
- Linker errors from moved functions -- update Makefile OBJS
- clang-tidy warnings in new files -- fix or add targeted NOLINT with reason
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/keyhunt && make clean all 2>&1 | tail -3 && echo "LINE_COUNT:" && wc -l src/keyhunt.cpp && echo "BSGS_EXTERNS:" && grep -c "^extern" src/modes/mode_bsgs.cpp && echo "IO_EXTERNS:" && grep -c "extern" src/io/io.cpp</automated>
  </verify>
  <done>keyhunt.cpp <= 600 lines, mode_bsgs.cpp < 30 externs, io.cpp 0 externs, all smoke tests pass, clang-tidy and cppcheck clean</done>
</task>

</tasks>

<verification>
1. `wc -l src/keyhunt.cpp` reports 600 or fewer
2. All 4 mode smoke tests pass (address, bsgs, xpoint, rmd160)
3. `make clang-tidy` exits 0
4. `make cppcheck` exits 0
5. Build with hardening flags compiles clean
</verification>

<success_criteria>
- keyhunt.cpp is 600 lines or fewer (STR-05 achieved)
- All search modes produce correct results
- Static analysis gates remain green
- No regression in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-monolith-decomposition/04-08-SUMMARY.md`
</output>
